
```swift
func example0() {
    let value = computeResult(input: 0)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


---

Text after a horizontal rule with `code` and **bold** and *italic*.

## Section heading at line 13

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


## Section heading at line 20

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


> This is a blockquote with some important information.
> It spans multiple lines and contains **bold** text.
> > And a nested blockquote for depth.


```javascript
func example32() {
    let value = computeResult(input: 32)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


> This is a blockquote with some important information.
> It spans multiple lines and contains **bold** text.
> > And a nested blockquote for depth.


## Section heading at line 53

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


A plain text paragraph at line 60. This tests the most common case — body text with no special markdown syntax, just flowing prose that might wrap across multiple lines depending on the window width.

A second paragraph with mixed `code spans`, **bold words**, and *italic phrases* to exercise inline token parsing.

---

Text after a horizontal rule with `code` and **bold** and *italic*.

- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


> This is a blockquote with some important information.
> It spans multiple lines and contains **bold** text.
> > And a nested blockquote for depth.


```swift
func example80() {
    let value = computeResult(input: 80)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


```javascript
func example89() {
    let value = computeResult(input: 89)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


> This is a blockquote with some important information.
> It spans multiple lines and contains **bold** text.
> > And a nested blockquote for depth.


> This is a blockquote with some important information.
> It spans multiple lines and contains **bold** text.
> > And a nested blockquote for depth.


A plain text paragraph at line 108. This tests the most common case — body text with no special markdown syntax, just flowing prose that might wrap across multiple lines depending on the window width.

A second paragraph with mixed `code spans`, **bold words**, and *italic phrases* to exercise inline token parsing.

### Section heading at line 112

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


```javascript
func example119() {
    let value = computeResult(input: 119)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


## Section heading at line 128

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


> This is a blockquote with some important information.
> It spans multiple lines and contains **bold** text.
> > And a nested blockquote for depth.


1. First ordered item
2. Second ordered item with **emphasis**
3. Third ordered item


> This is a blockquote with some important information.
> It spans multiple lines and contains **bold** text.
> > And a nested blockquote for depth.


> This is a blockquote with some important information.
> It spans multiple lines and contains **bold** text.
> > And a nested blockquote for depth.


- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


### Section heading at line 162

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


A plain text paragraph at line 169. This tests the most common case — body text with no special markdown syntax, just flowing prose that might wrap across multiple lines depending on the window width.

A second paragraph with mixed `code spans`, **bold words**, and *italic phrases* to exercise inline token parsing.

```go
func example173() {
    let value = computeResult(input: 173)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


## Section heading at line 203

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


> This is a blockquote with some important information.
> It spans multiple lines and contains **bold** text.
> > And a nested blockquote for depth.


## Section heading at line 215

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


```python
func example222() {
    let value = computeResult(input: 222)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


```go
func example231() {
    let value = computeResult(input: 231)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


---

Text after a horizontal rule with `code` and **bold** and *italic*.

## Section heading at line 244

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


# Section heading at line 251

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


```swift
func example258() {
    let value = computeResult(input: 258)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


> This is a blockquote with some important information.
> It spans multiple lines and contains **bold** text.
> > And a nested blockquote for depth.


- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


> This is a blockquote with some important information.
> It spans multiple lines and contains **bold** text.
> > And a nested blockquote for depth.


```rust
func example284() {
    let value = computeResult(input: 284)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


A plain text paragraph at line 300. This tests the most common case — body text with no special markdown syntax, just flowing prose that might wrap across multiple lines depending on the window width.

A second paragraph with mixed `code spans`, **bold words**, and *italic phrases* to exercise inline token parsing.

## Section heading at line 304

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


> This is a blockquote with some important information.
> It spans multiple lines and contains **bold** text.
> > And a nested blockquote for depth.


```python
func example316() {
    let value = computeResult(input: 316)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


A plain text paragraph at line 332. This tests the most common case — body text with no special markdown syntax, just flowing prose that might wrap across multiple lines depending on the window width.

A second paragraph with mixed `code spans`, **bold words**, and *italic phrases* to exercise inline token parsing.

---

Text after a horizontal rule with `code` and **bold** and *italic*.

- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


# Section heading at line 361

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


## Section heading at line 368

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


```swift
func example382() {
    let value = computeResult(input: 382)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


```go
func example391() {
    let value = computeResult(input: 391)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


1. First ordered item
2. Second ordered item with **emphasis**
3. Third ordered item


# Section heading at line 405

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


---

Text after a horizontal rule with `code` and **bold** and *italic*.

```javascript
func example416() {
    let value = computeResult(input: 416)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


## Section heading at line 425

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


### Section heading at line 439

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


```python
func example446() {
    let value = computeResult(input: 446)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


1. First ordered item
2. Second ordered item with **emphasis**
3. Third ordered item


> This is a blockquote with some important information.
> It spans multiple lines and contains **bold** text.
> > And a nested blockquote for depth.


1. First ordered item
2. Second ordered item with **emphasis**
3. Third ordered item


```rust
func example470() {
    let value = computeResult(input: 470)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


```swift
func example479() {
    let value = computeResult(input: 479)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


> This is a blockquote with some important information.
> It spans multiple lines and contains **bold** text.
> > And a nested blockquote for depth.


### Section heading at line 493

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.

