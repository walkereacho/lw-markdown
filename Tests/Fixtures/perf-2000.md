
A plain text paragraph at line 0. This tests the most common case — body text with no special markdown syntax, just flowing prose that might wrap across multiple lines depending on the window width.

A second paragraph with mixed `code spans`, **bold words**, and *italic phrases* to exercise inline token parsing.

- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


```rust
func example11() {
    let value = computeResult(input: 11)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


1. First ordered item
2. Second ordered item with **emphasis**
3. Third ordered item


- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


```python
func example53() {
    let value = computeResult(input: 53)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


# Section heading at line 62

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


# Section heading at line 69

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


```python
func example76() {
    let value = computeResult(input: 76)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


# Section heading at line 85

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


```swift
func example99() {
    let value = computeResult(input: 99)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


---

Text after a horizontal rule with `code` and **bold** and *italic*.

```javascript
func example112() {
    let value = computeResult(input: 112)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


1. First ordered item
2. Second ordered item with **emphasis**
3. Third ordered item


### Section heading at line 126

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


---

Text after a horizontal rule with `code` and **bold** and *italic*.

```rust
func example137() {
    let value = computeResult(input: 137)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


1. First ordered item
2. Second ordered item with **emphasis**
3. Third ordered item


- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


### Section heading at line 158

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


```rust
func example172() {
    let value = computeResult(input: 172)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


## Section heading at line 188

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


# Section heading at line 195

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


A plain text paragraph at line 202. This tests the most common case — body text with no special markdown syntax, just flowing prose that might wrap across multiple lines depending on the window width.

A second paragraph with mixed `code spans`, **bold words**, and *italic phrases* to exercise inline token parsing.

1. First ordered item
2. Second ordered item with **emphasis**
3. Third ordered item


### Section heading at line 211

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


---

Text after a horizontal rule with `code` and **bold** and *italic*.

---

Text after a horizontal rule with `code` and **bold** and *italic*.

---

Text after a horizontal rule with `code` and **bold** and *italic*.

# Section heading at line 230

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


---

Text after a horizontal rule with `code` and **bold** and *italic*.

```rust
func example241() {
    let value = computeResult(input: 241)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


# Section heading at line 250

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


> This is a blockquote with some important information.
> It spans multiple lines and contains **bold** text.
> > And a nested blockquote for depth.


- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


## Section heading at line 276

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


```javascript
func example283() {
    let value = computeResult(input: 283)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


### Section heading at line 299

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


# Section heading at line 306

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


```javascript
func example313() {
    let value = computeResult(input: 313)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


---

Text after a horizontal rule with `code` and **bold** and *italic*.

# Section heading at line 326

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


```swift
func example333() {
    let value = computeResult(input: 333)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


A plain text paragraph at line 342. This tests the most common case — body text with no special markdown syntax, just flowing prose that might wrap across multiple lines depending on the window width.

A second paragraph with mixed `code spans`, **bold words**, and *italic phrases* to exercise inline token parsing.

---

Text after a horizontal rule with `code` and **bold** and *italic*.

- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


```python
func example357() {
    let value = computeResult(input: 357)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


```go
func example366() {
    let value = computeResult(input: 366)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


```python
func example375() {
    let value = computeResult(input: 375)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


1. First ordered item
2. Second ordered item with **emphasis**
3. Third ordered item


A plain text paragraph at line 389. This tests the most common case — body text with no special markdown syntax, just flowing prose that might wrap across multiple lines depending on the window width.

A second paragraph with mixed `code spans`, **bold words**, and *italic phrases* to exercise inline token parsing.

```javascript
func example393() {
    let value = computeResult(input: 393)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


1. First ordered item
2. Second ordered item with **emphasis**
3. Third ordered item


- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


# Section heading at line 421

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


1. First ordered item
2. Second ordered item with **emphasis**
3. Third ordered item


---

Text after a horizontal rule with `code` and **bold** and *italic*.

- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


1. First ordered item
2. Second ordered item with **emphasis**
3. Third ordered item


1. First ordered item
2. Second ordered item with **emphasis**
3. Third ordered item


## Section heading at line 468

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


```rust
func example475() {
    let value = computeResult(input: 475)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


## Section heading at line 491

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


> This is a blockquote with some important information.
> It spans multiple lines and contains **bold** text.
> > And a nested blockquote for depth.


- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


```javascript
func example510() {
    let value = computeResult(input: 510)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


```python
func example519() {
    let value = computeResult(input: 519)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


1. First ordered item
2. Second ordered item with **emphasis**
3. Third ordered item


A plain text paragraph at line 533. This tests the most common case — body text with no special markdown syntax, just flowing prose that might wrap across multiple lines depending on the window width.

A second paragraph with mixed `code spans`, **bold words**, and *italic phrases* to exercise inline token parsing.

- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


### Section heading at line 544

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


---

Text after a horizontal rule with `code` and **bold** and *italic*.

```python
func example555() {
    let value = computeResult(input: 555)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


```rust
func example564() {
    let value = computeResult(input: 564)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


1. First ordered item
2. Second ordered item with **emphasis**
3. Third ordered item


---

Text after a horizontal rule with `code` and **bold** and *italic*.

A plain text paragraph at line 582. This tests the most common case — body text with no special markdown syntax, just flowing prose that might wrap across multiple lines depending on the window width.

A second paragraph with mixed `code spans`, **bold words**, and *italic phrases* to exercise inline token parsing.

- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


### Section heading at line 593

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


### Section heading at line 600

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


1. First ordered item
2. Second ordered item with **emphasis**
3. Third ordered item


1. First ordered item
2. Second ordered item with **emphasis**
3. Third ordered item


- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


### Section heading at line 631

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


```go
func example638() {
    let value = computeResult(input: 638)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


---

Text after a horizontal rule with `code` and **bold** and *italic*.

## Section heading at line 658

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


```python
func example665() {
    let value = computeResult(input: 665)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


```javascript
func example674() {
    let value = computeResult(input: 674)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


1. First ordered item
2. Second ordered item with **emphasis**
3. Third ordered item


# Section heading at line 695

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


```go
func example702() {
    let value = computeResult(input: 702)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


# Section heading at line 711

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


# Section heading at line 718

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


> This is a blockquote with some important information.
> It spans multiple lines and contains **bold** text.
> > And a nested blockquote for depth.


1. First ordered item
2. Second ordered item with **emphasis**
3. Third ordered item


A plain text paragraph at line 735. This tests the most common case — body text with no special markdown syntax, just flowing prose that might wrap across multiple lines depending on the window width.

A second paragraph with mixed `code spans`, **bold words**, and *italic phrases* to exercise inline token parsing.

- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


```python
func example746() {
    let value = computeResult(input: 746)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


---

Text after a horizontal rule with `code` and **bold** and *italic*.

# Section heading at line 766

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


1. First ordered item
2. Second ordered item with **emphasis**
3. Third ordered item


1. First ordered item
2. Second ordered item with **emphasis**
3. Third ordered item


```javascript
func example783() {
    let value = computeResult(input: 783)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


1. First ordered item
2. Second ordered item with **emphasis**
3. Third ordered item


## Section heading at line 797

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


## Section heading at line 804

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


# Section heading at line 811

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


1. First ordered item
2. Second ordered item with **emphasis**
3. Third ordered item


- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


A plain text paragraph at line 830. This tests the most common case — body text with no special markdown syntax, just flowing prose that might wrap across multiple lines depending on the window width.

A second paragraph with mixed `code spans`, **bold words**, and *italic phrases* to exercise inline token parsing.

# Section heading at line 834

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


```rust
func example841() {
    let value = computeResult(input: 841)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


1. First ordered item
2. Second ordered item with **emphasis**
3. Third ordered item


A plain text paragraph at line 862. This tests the most common case — body text with no special markdown syntax, just flowing prose that might wrap across multiple lines depending on the window width.

A second paragraph with mixed `code spans`, **bold words**, and *italic phrases* to exercise inline token parsing.

# Section heading at line 866

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


```swift
func example873() {
    let value = computeResult(input: 873)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


1. First ordered item
2. Second ordered item with **emphasis**
3. Third ordered item


# Section heading at line 887

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


```python
func example894() {
    let value = computeResult(input: 894)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


```rust
func example903() {
    let value = computeResult(input: 903)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


A plain text paragraph at line 912. This tests the most common case — body text with no special markdown syntax, just flowing prose that might wrap across multiple lines depending on the window width.

A second paragraph with mixed `code spans`, **bold words**, and *italic phrases* to exercise inline token parsing.

## Section heading at line 916

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


> This is a blockquote with some important information.
> It spans multiple lines and contains **bold** text.
> > And a nested blockquote for depth.


1. First ordered item
2. Second ordered item with **emphasis**
3. Third ordered item


A plain text paragraph at line 933. This tests the most common case — body text with no special markdown syntax, just flowing prose that might wrap across multiple lines depending on the window width.

A second paragraph with mixed `code spans`, **bold words**, and *italic phrases* to exercise inline token parsing.

- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


```javascript
func example951() {
    let value = computeResult(input: 951)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


### Section heading at line 960

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


## Section heading at line 967

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


# Section heading at line 974

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


# Section heading at line 981

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


> This is a blockquote with some important information.
> It spans multiple lines and contains **bold** text.
> > And a nested blockquote for depth.


```rust
func example993() {
    let value = computeResult(input: 993)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


---

Text after a horizontal rule with `code` and **bold** and *italic*.

```swift
func example1020() {
    let value = computeResult(input: 1020)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


> This is a blockquote with some important information.
> It spans multiple lines and contains **bold** text.
> > And a nested blockquote for depth.


- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


A plain text paragraph at line 1041. This tests the most common case — body text with no special markdown syntax, just flowing prose that might wrap across multiple lines depending on the window width.

A second paragraph with mixed `code spans`, **bold words**, and *italic phrases* to exercise inline token parsing.

- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


> This is a blockquote with some important information.
> It spans multiple lines and contains **bold** text.
> > And a nested blockquote for depth.


## Section heading at line 1064

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


A plain text paragraph at line 1071. This tests the most common case — body text with no special markdown syntax, just flowing prose that might wrap across multiple lines depending on the window width.

A second paragraph with mixed `code spans`, **bold words**, and *italic phrases* to exercise inline token parsing.

A plain text paragraph at line 1075. This tests the most common case — body text with no special markdown syntax, just flowing prose that might wrap across multiple lines depending on the window width.

A second paragraph with mixed `code spans`, **bold words**, and *italic phrases* to exercise inline token parsing.

## Section heading at line 1079

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


```python
func example1086() {
    let value = computeResult(input: 1086)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


## Section heading at line 1102

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


A plain text paragraph at line 1109. This tests the most common case — body text with no special markdown syntax, just flowing prose that might wrap across multiple lines depending on the window width.

A second paragraph with mixed `code spans`, **bold words**, and *italic phrases* to exercise inline token parsing.

```python
func example1113() {
    let value = computeResult(input: 1113)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


## Section heading at line 1122

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


1. First ordered item
2. Second ordered item with **emphasis**
3. Third ordered item


```javascript
func example1134() {
    let value = computeResult(input: 1134)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


## Section heading at line 1143

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


```rust
func example1150() {
    let value = computeResult(input: 1150)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


A plain text paragraph at line 1159. This tests the most common case — body text with no special markdown syntax, just flowing prose that might wrap across multiple lines depending on the window width.

A second paragraph with mixed `code spans`, **bold words**, and *italic phrases* to exercise inline token parsing.

### Section heading at line 1163

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


```javascript
func example1170() {
    let value = computeResult(input: 1170)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


```go
func example1179() {
    let value = computeResult(input: 1179)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


---

Text after a horizontal rule with `code` and **bold** and *italic*.

> This is a blockquote with some important information.
> It spans multiple lines and contains **bold** text.
> > And a nested blockquote for depth.


# Section heading at line 1197

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


```go
func example1204() {
    let value = computeResult(input: 1204)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


### Section heading at line 1213

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


```python
func example1220() {
    let value = computeResult(input: 1220)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


```go
func example1229() {
    let value = computeResult(input: 1229)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


```rust
func example1238() {
    let value = computeResult(input: 1238)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


```javascript
func example1254() {
    let value = computeResult(input: 1254)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


```swift
func example1263() {
    let value = computeResult(input: 1263)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


A plain text paragraph at line 1272. This tests the most common case — body text with no special markdown syntax, just flowing prose that might wrap across multiple lines depending on the window width.

A second paragraph with mixed `code spans`, **bold words**, and *italic phrases* to exercise inline token parsing.

- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


1. First ordered item
2. Second ordered item with **emphasis**
3. Third ordered item


```go
func example1288() {
    let value = computeResult(input: 1288)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


```swift
func example1297() {
    let value = computeResult(input: 1297)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


---

Text after a horizontal rule with `code` and **bold** and *italic*.

- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


> This is a blockquote with some important information.
> It spans multiple lines and contains **bold** text.
> > And a nested blockquote for depth.


1. First ordered item
2. Second ordered item with **emphasis**
3. Third ordered item


### Section heading at line 1341

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


1. First ordered item
2. Second ordered item with **emphasis**
3. Third ordered item


- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


1. First ordered item
2. Second ordered item with **emphasis**
3. Third ordered item


## Section heading at line 1365

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


A plain text paragraph at line 1372. This tests the most common case — body text with no special markdown syntax, just flowing prose that might wrap across multiple lines depending on the window width.

A second paragraph with mixed `code spans`, **bold words**, and *italic phrases* to exercise inline token parsing.

# Section heading at line 1376

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


```python
func example1383() {
    let value = computeResult(input: 1383)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


```python
func example1392() {
    let value = computeResult(input: 1392)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


A plain text paragraph at line 1401. This tests the most common case — body text with no special markdown syntax, just flowing prose that might wrap across multiple lines depending on the window width.

A second paragraph with mixed `code spans`, **bold words**, and *italic phrases* to exercise inline token parsing.

1. First ordered item
2. Second ordered item with **emphasis**
3. Third ordered item


> This is a blockquote with some important information.
> It spans multiple lines and contains **bold** text.
> > And a nested blockquote for depth.


1. First ordered item
2. Second ordered item with **emphasis**
3. Third ordered item


> This is a blockquote with some important information.
> It spans multiple lines and contains **bold** text.
> > And a nested blockquote for depth.


```javascript
func example1425() {
    let value = computeResult(input: 1425)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


1. First ordered item
2. Second ordered item with **emphasis**
3. Third ordered item


> This is a blockquote with some important information.
> It spans multiple lines and contains **bold** text.
> > And a nested blockquote for depth.


### Section heading at line 1444

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


# Section heading at line 1451

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


### Section heading at line 1458

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


## Section heading at line 1465

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


1. First ordered item
2. Second ordered item with **emphasis**
3. Third ordered item


### Section heading at line 1484

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


1. First ordered item
2. Second ordered item with **emphasis**
3. Third ordered item


```python
func example1496() {
    let value = computeResult(input: 1496)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


```go
func example1505() {
    let value = computeResult(input: 1505)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


### Section heading at line 1514

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


> This is a blockquote with some important information.
> It spans multiple lines and contains **bold** text.
> > And a nested blockquote for depth.


A plain text paragraph at line 1526. This tests the most common case — body text with no special markdown syntax, just flowing prose that might wrap across multiple lines depending on the window width.

A second paragraph with mixed `code spans`, **bold words**, and *italic phrases* to exercise inline token parsing.

### Section heading at line 1530

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


---

Text after a horizontal rule with `code` and **bold** and *italic*.

- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


A plain text paragraph at line 1548. This tests the most common case — body text with no special markdown syntax, just flowing prose that might wrap across multiple lines depending on the window width.

A second paragraph with mixed `code spans`, **bold words**, and *italic phrases* to exercise inline token parsing.

---

Text after a horizontal rule with `code` and **bold** and *italic*.

## Section heading at line 1556

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


A plain text paragraph at line 1563. This tests the most common case — body text with no special markdown syntax, just flowing prose that might wrap across multiple lines depending on the window width.

A second paragraph with mixed `code spans`, **bold words**, and *italic phrases* to exercise inline token parsing.

---

Text after a horizontal rule with `code` and **bold** and *italic*.

### Section heading at line 1571

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


1. First ordered item
2. Second ordered item with **emphasis**
3. Third ordered item


```python
func example1590() {
    let value = computeResult(input: 1590)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


## Section heading at line 1599

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


```javascript
func example1606() {
    let value = computeResult(input: 1606)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


---

Text after a horizontal rule with `code` and **bold** and *italic*.

### Section heading at line 1619

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


```rust
func example1640() {
    let value = computeResult(input: 1640)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


# Section heading at line 1649

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


```swift
func example1656() {
    let value = computeResult(input: 1656)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


# Section heading at line 1665

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


> This is a blockquote with some important information.
> It spans multiple lines and contains **bold** text.
> > And a nested blockquote for depth.


```python
func example1677() {
    let value = computeResult(input: 1677)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


## Section heading at line 1693

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


A plain text paragraph at line 1700. This tests the most common case — body text with no special markdown syntax, just flowing prose that might wrap across multiple lines depending on the window width.

A second paragraph with mixed `code spans`, **bold words**, and *italic phrases* to exercise inline token parsing.

- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


```python
func example1711() {
    let value = computeResult(input: 1711)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


### Section heading at line 1720

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


1. First ordered item
2. Second ordered item with **emphasis**
3. Third ordered item


- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


```go
func example1739() {
    let value = computeResult(input: 1739)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


```python
func example1748() {
    let value = computeResult(input: 1748)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


A plain text paragraph at line 1757. This tests the most common case — body text with no special markdown syntax, just flowing prose that might wrap across multiple lines depending on the window width.

A second paragraph with mixed `code spans`, **bold words**, and *italic phrases* to exercise inline token parsing.

# Section heading at line 1761

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


> This is a blockquote with some important information.
> It spans multiple lines and contains **bold** text.
> > And a nested blockquote for depth.


> This is a blockquote with some important information.
> It spans multiple lines and contains **bold** text.
> > And a nested blockquote for depth.


```go
func example1778() {
    let value = computeResult(input: 1778)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


### Section heading at line 1787

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


# Section heading at line 1794

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


> This is a blockquote with some important information.
> It spans multiple lines and contains **bold** text.
> > And a nested blockquote for depth.


```swift
func example1806() {
    let value = computeResult(input: 1806)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


1. First ordered item
2. Second ordered item with **emphasis**
3. Third ordered item


# Section heading at line 1820

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


1. First ordered item
2. Second ordered item with **emphasis**
3. Third ordered item


A plain text paragraph at line 1832. This tests the most common case — body text with no special markdown syntax, just flowing prose that might wrap across multiple lines depending on the window width.

A second paragraph with mixed `code spans`, **bold words**, and *italic phrases* to exercise inline token parsing.

# Section heading at line 1836

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


### Section heading at line 1843

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


1. First ordered item
2. Second ordered item with **emphasis**
3. Third ordered item


## Section heading at line 1855

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


A plain text paragraph at line 1862. This tests the most common case — body text with no special markdown syntax, just flowing prose that might wrap across multiple lines depending on the window width.

A second paragraph with mixed `code spans`, **bold words**, and *italic phrases* to exercise inline token parsing.

---

Text after a horizontal rule with `code` and **bold** and *italic*.

A plain text paragraph at line 1870. This tests the most common case — body text with no special markdown syntax, just flowing prose that might wrap across multiple lines depending on the window width.

A second paragraph with mixed `code spans`, **bold words**, and *italic phrases* to exercise inline token parsing.

# Section heading at line 1874

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


> This is a blockquote with some important information.
> It spans multiple lines and contains **bold** text.
> > And a nested blockquote for depth.


- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


---

Text after a horizontal rule with `code` and **bold** and *italic*.

- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


A plain text paragraph at line 1904. This tests the most common case — body text with no special markdown syntax, just flowing prose that might wrap across multiple lines depending on the window width.

A second paragraph with mixed `code spans`, **bold words**, and *italic phrases* to exercise inline token parsing.

- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


### Section heading at line 1915

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


A plain text paragraph at line 1922. This tests the most common case — body text with no special markdown syntax, just flowing prose that might wrap across multiple lines depending on the window width.

A second paragraph with mixed `code spans`, **bold words**, and *italic phrases* to exercise inline token parsing.

- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


- First item in the list with **bold** content
- Second item with `inline code` here
- Third item with *italic* text
  - Nested item under third
  - Another nested item


1. First ordered item
2. Second ordered item with **emphasis**
3. Third ordered item


---

Text after a horizontal rule with `code` and **bold** and *italic*.

A plain text paragraph at line 1949. This tests the most common case — body text with no special markdown syntax, just flowing prose that might wrap across multiple lines depending on the window width.

A second paragraph with mixed `code spans`, **bold words**, and *italic phrases* to exercise inline token parsing.

```go
func example1953() {
    let value = computeResult(input: 1953)
    guard value > 0 else { return }
    print("Result: \(value)")
}
```


---

Text after a horizontal rule with `code` and **bold** and *italic*.

1. First ordered item
2. Second ordered item with **emphasis**
3. Third ordered item


### Section heading at line 1971

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


A plain text paragraph at line 1978. This tests the most common case — body text with no special markdown syntax, just flowing prose that might wrap across multiple lines depending on the window width.

A second paragraph with mixed `code spans`, **bold words**, and *italic phrases* to exercise inline token parsing.

## Section heading at line 1982

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


### Section heading at line 1989

This is a regular paragraph with some text content. It contains **bold text** and *italic text* and some `inline code` for good measure. This line is intentionally longer to test wrapping behavior in the editor window.

Another paragraph follows with a [link](https://example.com) and more ***bold italic*** formatting mixed in with regular text content.


> This is a blockquote with some important information.
> It spans multiple lines and contains **bold** text.
> > And a nested blockquote for depth.

